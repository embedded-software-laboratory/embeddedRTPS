/*
 *
 * Author: Andreas Wüstenberg (andreas.wuestenberg@rwth-aachen.de)
 */

#ifndef RTPS_MESSAGE_H
#define RTPS_MESSAGE_H

#include <cstdint>

namespace rtps{
    class Message{
        /*
    public:
        Message(const uint8_t* buffer, const uint32_t& length);
        template <typename T>
        T read();

    private:
        const uint8_t* mwp_buffer;
        const uint32_t m_length;
        uint32_t m_nextPos = 0;
         */
    };
}

#endif //RTPS_MESSAGE_H
